<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.CallIdle" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <!-- Secondary brand color. -->
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimary</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="dialogs_btn_style">
        <item name="android:layout_marginTop">@dimen/dp10h</item>
        <item name="android:textSize">@dimen/sp10</item>
        <item name="android:gravity">center</item>
        <item name="android:text">Ok</item>
        <item name="android:layout_width">@dimen/dp100w</item>
        <item name="android:layout_height">@dimen/dp40h</item>
        <item name="android:textAllCaps">false</item>
        <item name="android:textAlignment">center</item>
    </style>

</resources>